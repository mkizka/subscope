{
  "lexicon": 1,
  "id": "me.subsco.admin.verifyAccess",
  "defs": {
    "main": {
      "type": "query",
      "description": "Verify access to the admin panel and return the server state and user permissions.",
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["status"],
          "properties": {
            "status": {
              "type": "string",
              "enum": ["needsSetup", "authorized", "unauthorized"],
              "description": "Access status: needsSetup (admin not registered), authorized (has access), unauthorized (not an admin)"
            }
          }
        }
      }
    }
  }
}
