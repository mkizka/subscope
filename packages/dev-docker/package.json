{
  "name": "@dawn/dev-docker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wait-on http://localhost:2583/xrpc/_health && docker compose down jetstream -v && docker compose up"
  },
  "devDependencies": {
    "wait-on": "8.0.2"
  }
}
