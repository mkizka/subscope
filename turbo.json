{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build", "generate"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "inputs": ["**/eslint.config.mjs", "**/*.{ts,tsx}"]
    },
    "dev": {
      "dependsOn": ["generate"],
      "cache": false
    },
    "generate": {
      "cache": false
    }
  }
}
