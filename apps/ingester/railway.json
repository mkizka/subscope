{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm build --filter @repo/ingester",
    "watchPatterns": [
      "/apps/ingester/**/*",
      "/packages/client/**/*",
      "/packages/common/**/*",
      "pnpm-lock.yaml"
    ]
  },
  "deploy": {
    "startCommand": "pnpm --filter @repo/ingester start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "limitOverride": {
      "containers": {
        "cpu": 1,
        "memoryBytes": 1000000000
      }
    }
  }
}
